{
  "devDependencies": {
    "concurrently": "^9.2.1"
  },
  "private": true,
  "scripts": {
    "dev": "concurrently \"npm run build:watch --prefix Frontend\" \"dotnet watch --project Taskboard\"",
    "build": "npm run build:watch --prefix Frontend",
    "start": "dotnet run --project Taskboard"
  }
}
